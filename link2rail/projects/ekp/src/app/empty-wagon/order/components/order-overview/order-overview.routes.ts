import { Routes } from '@angular/router';
import { OrderOverviewComponent} from './order-overview.component';

export const orderOverviewRoutes: Routes = [
    {
        path: '',
        component: OrderOverviewComponent
    }
];