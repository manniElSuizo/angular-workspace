import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewOrderTransportComponent } from './new-order-transport.component';
/*
describe('NewOrderTransportComponent', () => {
  let component: NewOrderTransportComponent;
  let fixture: ComponentFixture<NewOrderTransportComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [NewOrderTransportComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NewOrderTransportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
*/
