import { WagonInformationUtils } from './wagon-information-utils';

describe('WagonInformationUtils', () => {
  it('should create an instance', () => {
    expect(new WagonInformationUtils()).toBeTruthy();
  });
});
