import { TestBed } from '@angular/core/testing';
/*
import { DynamicSessionStorageService } from './dynamic-session-storage.service';

describe('DynamicSessionStorageService', () => {
  let service: DynamicSessionStorageService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(DynamicSessionStorageService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/