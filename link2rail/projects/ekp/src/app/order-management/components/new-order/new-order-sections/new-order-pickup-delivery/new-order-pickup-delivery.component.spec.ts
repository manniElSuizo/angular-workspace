import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewOrderPickupDeliveryComponent } from './new-order-pickup-delivery.component';
/*
describe('NewOrderPickupDeliveryComponent', () => {
  let component: NewOrderPickupDeliveryComponent;
  let fixture: ComponentFixture<NewOrderPickupDeliveryComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [NewOrderPickupDeliveryComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NewOrderPickupDeliveryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
