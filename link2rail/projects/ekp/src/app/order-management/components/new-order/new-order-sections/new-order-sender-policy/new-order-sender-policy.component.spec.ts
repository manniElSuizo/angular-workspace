import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewOrderSenderPolicyComponent } from './new-order-sender-policy.component';
/*
describe('NewOrderSenderPolicyComponent', () => {
  let component: NewOrderSenderPolicyComponent;
  let fixture: ComponentFixture<NewOrderSenderPolicyComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [NewOrderSenderPolicyComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NewOrderSenderPolicyComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
