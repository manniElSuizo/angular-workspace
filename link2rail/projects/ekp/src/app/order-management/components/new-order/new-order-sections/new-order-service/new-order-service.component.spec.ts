import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewOrderServiceComponent } from './new-order-service.component';
/*
describe('NewOrderServiceComponent', () => {
  let component: NewOrderServiceComponent;
  let fixture: ComponentFixture<NewOrderServiceComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [NewOrderServiceComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NewOrderServiceComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
*/