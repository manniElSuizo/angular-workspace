/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewOrderMainComponent } from './new-order-main.component';
import { NO_ERRORS_SCHEMA } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { TranslateModule } from '@ngx-translate/core';
import { ModalWindows } from '@src/app/shared/components/modal-windows/modal-windows';
import { MAT_DIALOG_DATA, MatDialogModule } from '@angular/material/dialog';

describe('NewOrderMainComponent', () => {
  let component: NewOrderMainComponent;
  let fixture: ComponentFixture<NewOrderMainComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [
        NewOrderMainComponent,
        TranslateModule.forRoot(),
        HttpClientModule
      ],
      providers: [
        { provide: MAT_DIALOG_DATA, useValue: { 'orderInfoData': { } } },
        ModalWindows
      ],
      schemas: [NO_ERRORS_SCHEMA]
    })
    .compileComponents();

    fixture = TestBed.createComponent(NewOrderMainComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
