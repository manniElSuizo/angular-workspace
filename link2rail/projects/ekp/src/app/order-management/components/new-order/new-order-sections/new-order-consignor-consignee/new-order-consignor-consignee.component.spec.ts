import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NewOrderConsignorConsigneeComponent } from './new-order-consignor-consignee.component';
/*
describe('NewOrderConsignorConsigneeComponent', () => {
  let component: NewOrderConsignorConsigneeComponent;
  let fixture: ComponentFixture<NewOrderConsignorConsigneeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [NewOrderConsignorConsigneeComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(NewOrderConsignorConsigneeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
