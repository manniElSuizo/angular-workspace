import { CustomerSgvPartnerIdPipe } from './customer-sgv-partner-id.pipe';

describe('CustomerSgvPartnerIdPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerSgvPartnerIdPipe();
    expect(pipe).toBeTruthy();
  });
});
