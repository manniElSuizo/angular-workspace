import { WagonNumberPipe } from './wagon-number.pipe';

describe('WagonNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WagonNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
