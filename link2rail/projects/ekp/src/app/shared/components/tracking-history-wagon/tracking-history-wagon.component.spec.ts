
/*import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrackingHistoryWagonComponent } from './tracking-history-wagon.component';

describe('TrackingHistoryWagonComponent', () => {
  let component: TrackingHistoryWagonComponent;
  let fixture: ComponentFixture<TrackingHistoryWagonComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TrackingHistoryWagonComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(TrackingHistoryWagonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
