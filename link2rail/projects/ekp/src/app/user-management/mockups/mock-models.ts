import { Tools } from "@src/app/trainorder/mockup/tools";
import { CustomerProfile } from "../model/profile.model";
import { Role } from "../model/role.model";
import { User, AccUserType } from "../model/user.model";
export class AccUserModelsMock {
    /*
    static userList(length: number = 5): AccUser[] {
        const list = [];
        for(let i = 0; i < length; i++) list.push(this.user());
        return list;
    }

    static user(name?: string, role?: Role, type?: AccUserType, partnerId?: number, sgv?: number): AccUser {
        return {
            type: AccUserType,
            role?: role,
            userName: name,
            activeProfiles: Profile[] = [],
            marketAreaClientNumber?: number,
            sgv?: number,
            partnerId?: number
        }
    }

}

export class ProfilesModelsMock {
    static profileList(length: number = 5): Profile[] {
        const list = [];
        for(let i = 0; i < length; i++) list.push(this.profile('0000'+(i+1), Number('0' + i + '000'), Number('00' + i + '00'), 'site_' + i));
        return list;
    }

    static profile(customerName: string, partnerId: number, sgvId: number, siteName: string): Profile {
        return {
            customerName: customerName,
            partnerId: partnerId,
            sgvId: sgvId,
            siteName: siteName
        }
    }
    */
}
