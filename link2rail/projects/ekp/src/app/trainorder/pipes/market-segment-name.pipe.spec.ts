import { MarketSegmentNamePipe } from "./market-segment-name.pipe";

 describe('MarketSegmentPipePipe', () => {
   it('create an instance', () => {
     const pipe = new MarketSegmentNamePipe();
     expect(pipe).toBeTruthy();
   });
 });
