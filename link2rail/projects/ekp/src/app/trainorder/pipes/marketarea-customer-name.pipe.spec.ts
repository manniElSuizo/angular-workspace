import { MarketareaCustomerNamePipe } from './marketarea-customer-name.pipe';

describe('MarketareaCustomerNamePipe', () => {
  it('create an instance', () => {
     const pipe = new MarketareaCustomerNamePipe();
     expect(pipe).toBeTruthy();
  });
});
